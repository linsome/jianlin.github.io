<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://jianlin.dev/feed.xml" rel="self" type="application/atom+xml"/><link href="https://jianlin.dev/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-25T01:20:14+00:00</updated><id>https://jianlin.dev/feed.xml</id><title type="html">Jian Lin</title><subtitle>Personal website of Jian Lin. Interested in Data Analysis and Artificial Intelligence. </subtitle><entry><title type="html">Reinforcement Learning — From Reward-Penalty Rules to Q-Learning</title><link href="https://jianlin.dev/machine-learning/2026/05/28/Refincorment_Learning.html" rel="alternate" type="text/html" title="Reinforcement Learning — From Reward-Penalty Rules to Q-Learning"/><published>2026-05-28T12:00:00+00:00</published><updated>2026-05-28T12:00:00+00:00</updated><id>https://jianlin.dev/machine-learning/2026/05/28/Refincorment_Learning</id><content type="html" xml:base="https://jianlin.dev/machine-learning/2026/05/28/Refincorment_Learning.html"><![CDATA[<p>In reinforcement learning (RL), an agent receives a reward of \(+1\) for a correct output and a penalty of \(-1\) for an incorrect output. RL tasks can be categorized into <strong>non-associative tasks</strong> and <strong>associative tasks</strong> — most practical discussions focus on associative tasks. The ultimate goal is to <strong>maximize the cumulative expected reward</strong> obtained by the model.</p> <hr/> <h2 id="reward-penalty-rule">Reward-Penalty Rule</h2> <p>The reward-penalty rule governs how weights are updated based on feedback:</p> \[\delta w_{mn} = \alpha \begin{cases} [y_m - \tanh(\beta b_m)]x_n &amp; \text{for } r = +1 \\ -\delta[y_m + \tanh(\beta b_m)]x_n &amp; \text{for } r = -1 \end{cases}\] <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/rl1-480.webp 480w,/assets/img/rl1-800.webp 800w,/assets/img/rl1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/rl1.png" class="img-fluid rounded" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Here \(\delta\) denotes the penalty coefficient. A smaller penalty yields closer convergence to the theoretical optimal reward. When the model performs correctly, a large reward is applied; when it errors, only a minimal correction is made. This design ensures that the network strongly follows positive feedback — analogous to reinforcement concepts in psychology. In the formula, \(\delta\) adjusts the weights in the opposite direction of \(y_m\).</p> <blockquote> <p><strong>Note:</strong> For the reward function to converge, the input dimension must exceed the number of samples.</p> </blockquote> <hr/> <h2 id="temporal-difference-td-learning">Temporal Difference (TD) Learning</h2> <p>Summing all actual future rewards directly is impractical in engineering because it requires completing an entire episode to compute all subsequent rewards. Instead, we define the <strong>value estimation function</strong>:</p> \[O(\mathbf{s}_t) = \mathbf{w} \cdot \mathbf{s}_t \tag{11.13}\] <p>Where \(O(\mathbf{s}_t)\) is the model’s estimate of the expected cumulative future reward for state \(\mathbf{s}_t\). We define a squared error loss function \(H\) over the sequence:</p> \[H = \frac{1}{2}\sum_{t=0}^{T-1}[R_t - O(\mathbf{s}_t)]^2 \tag{11.14}\] <p>Taking the gradient to update the weights yields:</p> \[\delta w_m = \alpha \sum_{t=0}^{T-1}[R_t - O(\mathbf{s}_t)]\frac{\partial O}{\partial w_m} \tag{11.15}\] <p>The prediction error can be mathematically rewritten by decomposing the target into successive time steps:</p> \[R_t - O(\mathbf{s}_t) = \sum_{\tau=t}^{T-1}[r_{\tau+1} + O(\mathbf{s}_{\tau+1}) - O(\mathbf{s}_\tau)] \tag{11.16}\] \[\delta \mathbf{w} = \alpha \sum_{t=0}^{T-1}\sum_{\tau=t}^{T-1}[r_{\tau+1} + O(\mathbf{s}_{\tau+1}) - O(\mathbf{s}_\tau)]\mathbf{s}_t \tag{11.17}\] <p>The term \(r_{\tau+1} + O(\mathbf{s}_{\tau+1}) - O(\mathbf{s}_\tau)\) is the <strong>TD error</strong>.</p> <hr/> <h2 id="tdλ--eligibility-traces">TD(λ) — Eligibility Traces</h2> <p>Equation (11.17) requires knowing future states during the summation. To make this computationally feasible, mathematicians altered the summation order. Instead of computing forward from \(t=0\) at the end of an episode, the <strong>lookback mechanism</strong> calculates incrementally at each current step by updating an eligibility trace of past steps.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/rl2-480.webp 480w,/assets/img/rl2-800.webp 800w,/assets/img/rl2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/rl2.png" class="img-fluid rounded" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>This reformulation leads to the \(\text{TD}(\lambda)\) weight update rule:</p> \[\delta \mathbf{w} = \alpha \sum_{t=0}^{T-1}[r_{t+1} + O(\mathbf{s}_{t+1}) - O(\mathbf{s}_t)]\sum_{\tau=0}^{t}\lambda^{t-\tau}\mathbf{s}_\tau \tag{11.19}\] <p>By updating weights online at each individual time step:</p> \[\delta \mathbf{w}_t = \alpha[r_{t+1} + O(\mathbf{w}_t, \mathbf{s}_{t+1}) - O(\mathbf{w}_t, \mathbf{s}_t)]\sum_{\tau=0}^{t}\lambda^{t-\tau}\mathbf{s}_\tau \tag{11.20}\] <p>The parameter \(\lambda\) is the discount tracking factor for past states \((0 \le \lambda \le 1)\). If \(\lambda = 0.5\), the influence of states further in the past decays exponentially. If \(\lambda = 0\), the algorithm becomes <strong>TD(0)</strong> — updating based solely on the immediate next state transition. TD(0) is widely applied in robotic training and control.</p> <hr/> <h2 id="sarsa-and-q-learning">SARSA and Q-Learning</h2> <p>In the TD(0) framework, value estimation is coupled with a policy through the Q-function \(Q(\mathbf{s}, \mathbf{a})\):</p> \[Q_{t+1}(\mathbf{s}_t, \mathbf{a}_t) = Q_t(\mathbf{s}_t, \mathbf{a}_t) + \alpha_t [r_{t+1} + \gamma Q_t(\mathbf{s}_{t+1}, \mathbf{a}_{t+1}) - Q_t(\mathbf{s}_t, \mathbf{a}_t)] \tag{11.23}\] <p>Equation (11.23) is the update rule for <strong>SARSA</strong>, an on-policy TD algorithm that evaluates the Q-table using the <strong>actual next action</strong> \(\mathbf{a}_{t+1}\) chosen by the current policy.</p> <p>Closely related is <strong>Q-learning</strong>, an off-policy algorithm where the update uses the maximum potential reward of the next state \(\max_{\mathbf{a}} Q_t(\mathbf{s}_{t+1}, \mathbf{a})\), independent of the policy’s actual next choice.</p> <p>These value-based TD methods serve as the foundational principles for game-playing strategies — from Tic-Tac-Toe all the way up to value-network architectures in Deep RL systems like <strong>AlphaGo</strong>.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="deep-learning"/><category term="reinforcement-learning"/><category term="TD-learning"/><category term="Q-learning"/><summary type="html"><![CDATA[A structured walkthrough of core RL concepts, from the reward-penalty weight update rule to Temporal Difference learning, SARSA, and Q-learning.]]></summary></entry><entry><title type="html">Unsupervised Learning — Neural Networks vs. Classical Machine Learning</title><link href="https://jianlin.dev/machine-learning/2026/05/25/Unsupervise.html" rel="alternate" type="text/html" title="Unsupervised Learning — Neural Networks vs. Classical Machine Learning"/><published>2026-05-25T12:00:00+00:00</published><updated>2026-05-25T12:00:00+00:00</updated><id>https://jianlin.dev/machine-learning/2026/05/25/Unsupervise</id><content type="html" xml:base="https://jianlin.dev/machine-learning/2026/05/25/Unsupervise.html"><![CDATA[<p>This post is a comparative summary of unsupervised learning, bridging Chapter 10 of <em>Machine Learning with Neural Networks</em> with classical machine learning techniques — examining how both paradigms tackle the same fundamental problem: learning structure from unlabeled data.</p> <hr/> <h2 id="part-1-neural-network-perspective">Part 1: Neural Network Perspective</h2> <h3 id="ojas-rule--online-pca">Oja’s Rule — Online PCA</h3> <p>Oja’s Rule is the neural network implementation of <strong>Principal Component Analysis (PCA)</strong>. Named after Finnish scientist Erkki Oja, its key distinction from classical PCA is how it processes data: rather than computing a covariance matrix over the entire dataset at once, Oja’s Rule updates weights <strong>incrementally with each new sample</strong>.</p> <p>This online learning approach dramatically reduces memory pressure, making it practical for large-scale or streaming data settings. By extending this idea through <strong>Sanger’s Rule</strong> (also known as the Generalized Hebbian Algorithm), the network can sequentially extract higher-order principal components — the second, third, and beyond.</p> <h3 id="competitive-learning--winner-takes-all">Competitive Learning — Winner Takes All</h3> <p>Competitive learning operates on a simple but powerful principle: during training, only the <strong>winning neuron</strong> — the one whose weight vector lies closest to the current input — is permitted to update its weights. All other neurons remain unchanged.</p> <p>This winner-take-all mechanism naturally encourages neurons to specialize, with each neuron gradually becoming an expert on a particular region of the input space.</p> <h3 id="self-organizing-maps-som--topology-preserving-dimensionality-reduction">Self-Organizing Maps (SOM) — Topology-Preserving Dimensionality Reduction</h3> <p>While PCA is inherently linear, <strong>Self-Organizing Maps (SOM)</strong> are designed to capture <strong>non-linear structures</strong> in data. The key extension over competitive learning: SOM updates not just the winning neuron, but also its <strong>topological neighbors</strong>, governed by a neighborhood function \(h(i, i_0)\).</p> <p>When projecting high-dimensional data onto a 2D grid, SOM behaves like an elastic net — the winning neuron pulls its neighbors toward the input at each step, gradually unfolding the data manifold. However, SOM has a well-known limitation: the <strong>boundary effect</strong>, where representation quality degrades near the edges of the grid.</p> <hr/> <h2 id="part-2-classical-machine-learning-perspective">Part 2: Classical Machine Learning Perspective</h2> <h3 id="multidimensional-scaling-mds">Multidimensional Scaling (MDS)</h3> <p>The classical ML analog to SOM is <strong>Multidimensional Scaling (MDS)</strong>, which attempts to preserve <strong>global pairwise distances</strong> — ensuring that the distances between all pairs of points in the low-dimensional space match their original high-dimensional distances.</p> <p>While principled, this global optimization comes at a significant computational cost: MDS must compute all pairwise distances simultaneously, scaling poorly with dataset size.</p> <h3 id="t-sne--probabilistic-neighbor-embedding">t-SNE — Probabilistic Neighbor Embedding</h3> <p><strong>t-SNE</strong> improves upon linear baselines like Kernel PCA by leveraging the <strong>Student-t distribution</strong>. It computes the ratio of joint probability distributions of pairwise distances in the high-dimensional versus low-dimensional space, then minimizes the KL divergence between them.</p> <p>The result: far superior clustering and visualization performance, especially for high-dimensional data like image embeddings.</p> <hr/> <h2 id="part-3-computational-trade-offs">Part 3: Computational Trade-offs</h2> <table> <thead> <tr> <th>Method</th> <th>Update Strategy</th> <th>Complexity</th> </tr> </thead> <tbody> <tr> <td>SOM</td> <td>One sample vs. existing weights</td> <td>Low — local updates</td> </tr> <tr> <td>MDS</td> <td>All pairwise distances globally</td> <td>High — \(O(n^2)\)</td> </tr> <tr> <td>t-SNE</td> <td>Probabilistic pairwise ratios</td> <td>Medium — approximate methods needed for large \(n\)</td> </tr> </tbody> </table> <p>SOM’s computational advantage comes precisely from its local update rule — it never needs to see the entire dataset at once.</p> <hr/> <h2 id="part-4-bridge-models--deep-generative-models">Part 4: Bridge Models &amp; Deep Generative Models</h2> <h3 id="k-means-vs-radial-basis-functions-rbf">K-Means vs. Radial Basis Functions (RBF)</h3> <p>Both K-means and RBF networks partition data by geometric distance, but handle decision boundaries differently. K-means enforces <strong>hard assignments</strong> — each point belongs to exactly one cluster. RBF networks soften this by transforming distances into <strong>smooth probabilities</strong> via a Softmax-like normalization, producing more flexible boundaries.</p> <h3 id="autoencoders--learning-to-compress">Autoencoders — Learning to Compress</h3> <p>Standard <strong>Autoencoders (AE)</strong> introduce the concept of reconstruction learning: an encoder compresses the input into a low-dimensional bottleneck, and a decoder reconstructs the original input from that compressed representation. The training signal is simply reconstruction error.</p> <p><strong>Variational Autoencoders (VAE)</strong> take this further by imposing a <strong>Gaussian distribution</strong> on the latent space, regularized via <strong>KL Divergence</strong>. This constraint structures the latent space to be continuous and meaningful — enabling not just compression, but controlled generation.</p> <h3 id="the-road-to-modern-generative-ai">The Road to Modern Generative AI</h3> <p>VAEs laid the groundwork for today’s most powerful generative architectures:</p> <ul> <li><strong>GANs</strong> — pitting a generator against a discriminator in an adversarial game</li> <li><strong>Diffusion Models</strong> — learning to iteratively denoise data from pure noise</li> <li><strong>Stable Diffusion</strong> — combines <strong>VQ-VAE</strong> for spatial compression with a Diffusion Model for high-fidelity image synthesis</li> </ul> <p>The thread connecting all of these: learning compact, structured representations of data — the core goal of unsupervised learning.</p> <hr/> <p><em>From Oja’s online weight updates to latent diffusion models, unsupervised learning has evolved from elegant mathematical rules into the engine powering modern generative AI.</em></p>]]></content><author><name></name></author><category term="machine-learning"/><category term="deep-learning"/><category term="unsupervised-learning"/><category term="PCA"/><category term="SOM"/><category term="VAE"/><summary type="html"><![CDATA[A comparative review of unsupervised learning techniques across neural network and classical ML perspectives, from PCA and SOM to Autoencoders and Diffusion Models.]]></summary></entry><entry><title type="html">Supervised Recurrent Networks and the GRU to Vanishing Gradients</title><link href="https://jianlin.dev/machine-learning/2026/05/20/Recurrent.html" rel="alternate" type="text/html" title="Supervised Recurrent Networks and the GRU to Vanishing Gradients"/><published>2026-05-20T12:00:00+00:00</published><updated>2026-05-20T12:00:00+00:00</updated><id>https://jianlin.dev/machine-learning/2026/05/20/Recurrent</id><content type="html" xml:base="https://jianlin.dev/machine-learning/2026/05/20/Recurrent.html"><![CDATA[<p>Feedforward neural networks process each input independently — they have no memory of what came before. <strong>Recurrent Neural Networks (RNNs)</strong> break this limitation by introducing connections across time, making them naturally suited for sequential data like text, audio, and time series.</p> <h2 id="the-core-idea-recurrence">The Core Idea: Recurrence</h2> <p>The defining feature of an RNN is that the hidden state at time \(t\) depends not only on the current input, but also on the hidden state from the previous timestep:</p> \[V_i(t) = g\!\left(\sum_j w_{ij}^{(vv)} V_j(t-1) + \sum_k w_{ik}^{(vx)} x_k - \theta_i^{(v)}\right) \quad \text{for } t = 1, 2, \ldots\] <p>The term \(\sum_j w_{ij}^{(vv)} V_j(t-1)\) is what makes RNNs unique — it is a weighted sum over all hidden units from the <strong>previous timestep</strong>, effectively giving the network a form of memory.</p> <h2 id="backpropagation-through-time">Backpropagation Through Time</h2> <p>Training an RNN requires a modified version of backpropagation called <strong>Backpropagation Through Time (BPTT)</strong>. The weight update for the recurrent weights \(w_{mn}^{(vv)}\) is:</p> \[\delta w_{mn}^{(vv)} = -\eta \frac{\partial H}{\partial w_{mn}^{(vv)}} = \eta \sum_k E_k^* \frac{\partial V_k^*}{\partial w_{mn}^{(vv)}}\] <p>The key difference from standard backprop: the gradient must account for how neuron \(m\) influences <strong>all other neurons</strong> across time — not just the immediate next layer. This makes the gradient computation significantly more complex.</p> <h2 id="the-vanishing-gradient-problem">The Vanishing Gradient Problem</h2> <p>As gradients are propagated backwards through many timesteps, they are multiplied together repeatedly. If these values are smaller than 1, the gradients <strong>shrink exponentially</strong> — a phenomenon known as the <strong>vanishing gradient problem</strong>. In practice, this means the network struggles to learn long-range dependencies: information from many steps ago effectively disappears before it can influence the weights.</p> <h2 id="the-gru-solution-gated-recurrent-units">The GRU Solution: Gated Recurrent Units</h2> <p>The solution mirrors the intuition behind <strong>ResNet</strong> in computer vision — create a shortcut path that allows gradients to flow unchanged. <strong>Gated Recurrent Units (GRUs)</strong> achieve this through two learnable gates:</p> \[z_m(t) = \sigma\!\left(\sum_k w_{mk}^{(zx)} x_k(t) + \sum_j w_{mj}^{(zv)} V_j(t-1)\right) \tag{9.30a}\] \[r_n(t) = \sigma\!\left(\sum_k w_{nk}^{(rx)} x_k(t) + \sum_j w_{nj}^{(rv)} V_j(t-1)\right) \tag{9.30b}\] \[h_i(t) = g\!\left(\sum_k w_{ik}^{(hx)} x_k(t) + \sum_j w_{ij}^{(hv)} r_j(t) V_j(t-1)\right) \tag{9.30c}\] \[V_i(t) = [1 - z_i(t)]\, h_i(t) + z_i(t)\, V_i(t-1) \tag{9.30d}\] <p>The two gates play distinct roles:</p> <ul> <li><strong>Update gate \(z(t)\)</strong> — Controls how much <strong>new</strong> information to incorporate. Backed by a sigmoid activation, it outputs a value between 0 and 1.</li> <li><strong>Reset gate \(r(t)\)</strong> — Controls how much <strong>old</strong> memory to forget before computing the new candidate state.</li> </ul> <h3 id="the-resnet-analogy">The ResNet Analogy</h3> <p>Look closely at equation (9.30d):</p> \[V_i(t) = [1 - z_i(t)]\, h_i(t) + z_i(t)\, V_i(t-1)\] <p>When \(z(t) = 1\), this collapses to \(V_i(t) = V_i(t-1)\) — the hidden state is <strong>copied unchanged</strong> from the previous timestep, with no new information at all. This is precisely the skip connection idea from ResNet: the network can choose to pass the gradient through time without any transformation, preventing it from vanishing.</p> <h2 id="summary">Summary</h2> <table> <thead> <tr> <th>Problem</th> <th>Solution</th> </tr> </thead> <tbody> <tr> <td>Standard RNNs forget long-range context</td> <td>Recurrent weights carry state across timesteps</td> </tr> <tr> <td>Vanishing gradients block long-range learning</td> <td>GRU gates create identity shortcuts through time</td> </tr> </tbody> </table> <p>GRUs demonstrate a recurring theme in deep learning: when optimization is hard, <strong>architectural shortcuts</strong> often work better than algorithmic fixes. By giving the network an explicit path to preserve information unchanged, GRUs make learning long-range dependencies tractable — without the full complexity of LSTMs.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="deep-learning"/><category term="RNN"/><category term="GRU"/><category term="neural-networks"/><summary type="html"><![CDATA[A deep dive into how Recurrent Neural Networks handle sequential data]]></summary></entry><entry><title type="html">EM Algorithm for Gaussian Mixture Models</title><link href="https://jianlin.dev/machine-learning/2026/05/16/GMM.html" rel="alternate" type="text/html" title="EM Algorithm for Gaussian Mixture Models"/><published>2026-05-16T12:00:00+00:00</published><updated>2026-05-16T12:00:00+00:00</updated><id>https://jianlin.dev/machine-learning/2026/05/16/GMM</id><content type="html" xml:base="https://jianlin.dev/machine-learning/2026/05/16/GMM.html"><![CDATA[<p>In an era dominated by multi-billion parameter neural networks, it is easy to dismiss classical statistical models as relics of the past. However, during my recent assignment implementing a <strong>Gaussian Mixture Model (GMM)</strong> from scratch, I was reminded of the sheer mathematical elegance that powers these traditional techniques — and reintroduced to the <strong>Expectation-Maximization (EM) algorithm</strong>.</p> <h2 id="1-what-is-a-gmm">1. What is a GMM?</h2> <p>A GMM assumes all data points are generated from a mixture of Gaussian distributions. The probability density function of a sample \(\boldsymbol{x}_i \in \mathbb{R}^d\) is:</p> \[p(\boldsymbol{x}_i \mid \boldsymbol{\theta}) = \sum_{j=1}^{k} \pi_j \mathcal{N}(\boldsymbol{x}_i \;; \boldsymbol{\mu}_j, \boldsymbol{\Sigma}_j)\] <p>Where \(\pi_j\) is the mixing coefficient of the \(j\)-th cluster, and \(\boldsymbol{\mu}_j\), \(\boldsymbol{\Sigma}_j\) are the mean and covariance of each Gaussian component.</p> <p>GMMs are ideal for <strong>low-to-medium dimensional continuous data</strong>. Unlike hard clustering (k-means), GMMs perform <strong>soft clustering</strong> — assigning probabilities rather than rigid labels. In credit risk modeling, for example, a GMM might say <em>“this profile has a 75% chance of defaulting”</em> rather than forcing a binary decision.</p> <h2 id="2-the-core-magic-em-algorithm">2. The Core Magic: EM Algorithm</h2> <p>Fitting a GMM via Maximum Likelihood requires maximizing the log-likelihood:</p> \[\ell(\boldsymbol{\theta}) = \sum_{i=1}^{n} \log \left[ \sum_{j=1}^{k} \pi_j \mathcal{N}(\boldsymbol{x}_i \;; \boldsymbol{\mu}_j, \boldsymbol{\Sigma}_j) \right]\] <p>The problem: a <strong>logarithm of a sum</strong> that cannot be solved analytically. The EM algorithm bypasses this by introducing a responsibility matrix \(F_{ij}\) and using Jensen’s Inequality to construct a tractable lower bound:</p> \[\ell(\boldsymbol{\theta}) \ge \sum_{i=1}^{n} \sum_{j=1}^{k} F_{ij} \log \left[ \frac{p_{\boldsymbol{\theta}}(\boldsymbol{x}_i, Z_i=j)}{F_{ij}} \right] = Q(\mathbf{F}, \boldsymbol{\theta})\] <p>This transforms a messy \(\log \sum\) into a differentiable \(\sum \log\). The algorithm then alternates between two steps:</p> <p><strong>E-Step:</strong> Fix \(\boldsymbol{\theta}\), compute responsibilities \(F_{ij}\) — the probability that cluster \(j\) generated sample \(i\).</p> <p><strong>M-Step:</strong> Fix \(F_{ij}\), update parameters in closed form:</p> \[\boldsymbol{\mu}_j^{\text{new}} = \frac{\sum_{i=1}^{n} F_{ij} \boldsymbol{x}_i}{\sum_{i=1}^{n} F_{ij}} \quad , \quad \boldsymbol{\Sigma}_j^{\text{new}} = \frac{\sum_{i=1}^{n} F_{ij} (\boldsymbol{x}_i - \boldsymbol{\mu}_j)(\boldsymbol{x}_i - \boldsymbol{\mu}_j)^T}{\sum_{i=1}^{n} F_{ij}}\] <p>Every iteration strictly increases the log-likelihood until convergence.</p> <h2 id="3-implementation-challenges">3. Implementation Challenges</h2> <p>Two real-world pitfalls worth knowing:</p> <p><strong>Local Minima</strong> — The log-likelihood landscape is non-convex. Poor initialization leads to subpar solutions. Fix: run multiple random initializations and keep the best result.</p> <p><strong>Covariance Collapse</strong> — In high-dimensional data (e.g. flattened MNIST images), a cluster can collapse onto a single outlier, driving variance to zero and crashing the likelihood to infinity. Fix: clip the variance floor with <code class="language-plaintext highlighter-rouge">np.maximum(sigma_sq, 1e-2)</code>.</p> <h2 id="final-thoughts">Final Thoughts</h2> <p>Implementing EM from scratch offered a perspective that backpropagation never could — taking a mathematically intractable problem, approximating it with an elegant lower bound, and optimizing it through rigorous probabilistic reasoning.</p> <p>Sometimes, looking back at classical machine learning is exactly what you need to move forward as a better engineer.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="machine-learning"/><category term="statistics"/><category term="EM"/><category term="GMM"/><summary type="html"><![CDATA[Implementing GMM from scratch reminded me of the mathematical elegance behind classical statistical models — and why EM algorithm is an optimization masterpiece.]]></summary></entry></feed>